import { Dimension, NetDimensions } from '../models';

const DEFAULT_DIMENSION: Dimension = 8;

export const DEFAULT_NET_DIMENSION: NetDimensions = {
  x: DEFAULT_DIMENSION,
  y: DEFAULT_DIMENSION,
};
